// .repeat te va permitir duplicar las veces que quiera por param

let nombre = 'fran '.repeat(3)
console.log(`${nombre}`) // 3 veces
// si le pasa por parámetro un double, lo que hace es redondear


// .split() desmenuzar un String
let asignaturas = "ED,PROG,BD"
console.log(asignaturas.split(","))