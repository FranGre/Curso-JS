// argumentos y parámetros

function sumar(a, b) {
  // a y b son parámetros
  return a + b;
}

let resultado = sumar(2, 4); // 2 y 4 son los argumentos
console.log(resultado);

function saludar(nombre, apellido) {
  console.log(`Hola ${nombre} ${apellido} !!!`);
}

saludar("Fran", "Gregori");
